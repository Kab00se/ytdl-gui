package com.kevincyt.ytdlgui.customfx;

import com.kevincyt.ytdlgui.model.jobs.YtdlDownload;

import javafx.scene.control.ListView;

public class YtdlListView extends ListView<YtdlDownload> {

	/*
	 * TODO: ListView for YoutubeDL jobs
	 * 
	 * Couldn't access docs from train. IIRC need to have a ListCellFactory for
	 * YtdlDownload for this object to be able to create the cells. Not sure if
	 * it has a property to listen to or if I have to hook it up to an
	 * observable list I made (pretty sure it's the latter).
	 */
}
